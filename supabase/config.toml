# Minimal config so `npx supabase functions deploy invite-establishment` works.
project_id = "bmsftpvixpvtjrlclnlz"

[functions.invite-establishment]
verify_jwt = true

[functions.kira-ai]
verify_jwt = true
