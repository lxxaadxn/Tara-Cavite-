# Local schema keeps app icons/splash resolving when Expo API is unreachable.
# App data (Supabase) still works online.
# Offline mode is applied by scripts/run-expo.cjs for LAN starts only
# (tunnel needs Expo cloud — do not set EXPO_OFFLINE=1 here).
EXPO_UNIVERSE_DIR=./expo-offline-universe
