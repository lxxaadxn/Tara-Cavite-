/**
 * Admin dashboard lives at `/admin/*` on the same app as marketing web.
 * Sign in at `/login`; admin accounts are sent here after auth.
 */
export const ADMIN_APP_HOME_PATH = '/admin/web/dashboard';
