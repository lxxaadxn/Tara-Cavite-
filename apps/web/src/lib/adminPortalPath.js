/**
 * Admin dashboard lives at `/admin/*` on the same app as marketing web (`apps/web` + `apps/admin` routes).
 * Run once: `npm run dev` from repo root → http://localhost:5173/admin/web/dashboard
 */
export const ADMIN_APP_HOME_PATH = '/admin/web/dashboard';
